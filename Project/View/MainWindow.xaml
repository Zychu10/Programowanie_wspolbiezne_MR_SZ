<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800" Background="#3385b5" Foreground="Black" BorderBrush="Black">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Canvas Width="700" Height="400" VerticalAlignment="Top" HorizontalAlignment="Center" Background="#F5FACD" Margin="0, 134, 0, 0">
            <ItemsControl Name="Balls" ItemsSource="{Binding Balls}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Canvas>
                            <Ellipse Stroke="Black" Fill="#7310de"  Width="{Binding Size}" Height="{Binding Size}" Canvas.Left="{Binding X}" Canvas.Top="{Binding Y}"/>
                        </Canvas>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Border BorderBrush="#375573" BorderThickness="5,5,5,5" Height="400" Width="700" HorizontalAlignment="Center" VerticalAlignment="Top" >
            </Border>
        </Canvas>
        <TextBox HorizontalAlignment="Center" VerticalContentAlignment="Center" Margin="0,28,0,0" TextWrapping="Wrap" Text="{Binding BallVal}"  VerticalAlignment="Top" Width="200" Height="50" TextAlignment="Center" FontSize="30" BorderBrush="#375573" BorderThickness="5,5,5,5" />
        <Button Margin="50,28,530,476" Content="Start" FontSize="40" BorderThickness="5,5,5,5" BorderBrush="Black" Command="{Binding RunCommand}" IsEnabled="{Binding isRunEnabled}" ></Button>
        <Button x:Name= "StopButton" Margin="530,28,50,476" Content="Stop" BorderThickness="5,5,5,5" BorderBrush="Black" FontSize="40" Command="{Binding StopCommand}"></Button>
        <Button x:Name= "AddButton" Width="200" Height="30" Margin="250,-365,250,10" Content="Add" FontSize="20" Command="{Binding AddCommand}" IsEnabled="{Binding isAddEnabled}"></Button>
       
    </Grid>
</Window>